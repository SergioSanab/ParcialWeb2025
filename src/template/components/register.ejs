<!doctype html>
<html lang="es">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Registro - Proyectos</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body class="bg-light">

  <%- include('../partials/header') %>

  <%- include('../partials/menu') %>

  <main class="container py-5">
    <% if (typeof success !=='undefined' && success) { %>
                <div class="alert alert-success" role="alert">
                Your message has been sent successfully!
                </div>
                 <button onclick="location.href='/products/v1.0/list'" type="button" class="btn-close" ></button>
                <% } else { %>
    <div class="row justify-content-center">
      <div class="col-12 col-md-10 col-lg-8">
        <div class="card shadow-sm">
          <div class="card-body p-4 p-md-5">
            <h4 class="card-title mb-4 text-center">Registro de Proyecto</h4>

            <form id="projectForm" class="needs-validation" novalidate action="/Registers/v1.0/Register" method="post">

              <div class="mb-3">
                <label for="title" class="form-label">Título del Proyecto</label>
                <input type="text" class="form-control" id="title" name="title" required minlength="5">
                <div class="invalid-feedback">Ingrese un título de al menos 5 caracteres.</div>
              </div>

              <div class="row g-3 mb-3">
                <div class="col-md-6">
                  <label for="docente" class="form-label">Docente Guía</label>
                  <input type="text" class="form-control" id="docente" name="docente"
                    placeholder="Ej. Ing. Ana Pérez" required>
                  <div class="invalid-feedback">Ingrese el nombre del docente.</div>
                </div>
                <div class="col-md-6">
                  <label for="curso" class="form-label">Curso / Asignatura</label>
                  <input type="text" class="form-control" id="curso" name="curso"
                    placeholder="Ej. Programación Avanzada" required>
                  <div class="invalid-feedback">Ingrese el curso.</div>
                </div>
              </div>

              <div class="mb-3">
                <label for="fecha_y_hora" class="form-label">Fecha y Hora de Exposición</label>
                <input type="datetime-local" class="form-control" id="fecha_y_hora" name="fecha_y_hora" required>
                <div class="invalid-feedback">Seleccione una fecha y hora válidas.</div>
              </div>

              <div class="mb-3">
                <label for="expositores" class="form-label">Expositores</label>
                <textarea class="form-control" id="expositores" name="expositores" rows="3"
                  placeholder="Listar los nombres completos de los estudiantes..." required></textarea>
                <div class="form-text">Puede listar varios nombres, uno por línea.</div>
                <div class="invalid-feedback">Ingrese al menos un expositor.</div>
              </div>

              <div class="mb-3">
                <label for="description" class="form-label">Descripción del Proyecto</label>
                <textarea class="form-control" id="description" name="description" rows="4"
                  placeholder="Breve resumen de qué hace el proyecto, objetivos, etc."></textarea>
              </div>

              <div class="mb-3">
                <label for="comentarios" class="form-label">Comentarios / Observaciones (Opcional)</label>
                <textarea class="form-control" id="comentarios" name="comentarios" rows="3"
                  ></textarea>
              </div>

              <div class="d-grid pt-2">
                <button class="btn btn-primary btn-lg" type="submit" formaction="/Registers/v1.0/Register">Registrar Proyecto</button>
              </div>
            </form>

            <div id="successAlert" class="alert alert-success mt-3 d-none" role="alert">
              ¡Proyecto registrado correctamente!
            </div>
          </div>
        </div>
      </div>
    </div>
    <% } %>
  </main>

  <%- include('../partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
    crossorigin="anonymous"></script>

  <script>
    (() => {
      'use strict'
    
      const forms = document.querySelectorAll('.needs-validation')
    
      Array.from(forms).forEach(form => {
        form.addEventListener('submit', event => {
          if (!form.checkValidity()) {
            event.preventDefault()
            event.stopPropagation()
          }
    
          form.classList.add('was-validated')
        }, false)
      })
    })()
  </script>

  

</body>

</html>