<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Resultados para "<%= query %>"</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
    xintegrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
</head>

<body>

  <%- include('../partials/header') %>

  <%- include('../partials/menu') %>

  <div class="container py-4">

    <!-- Título que muestra el término de búsqueda -->
    <h2 class="mb-4">
      Resultados de búsqueda para: <span class="text-success fst-italic">"<%= query %>"</span>
    </h2>

    <div class="row g-4"> 
      <% if (products && products.length > 0) { %>
        <% products.forEach(product=> { %>
          
          <div class="col-12 col-md-6 col-lg-4 d-flex align-items-stretch">
            <div class="card h-100 shadow-sm">
              <img 
                src="/assets/img/<%= product.id %>.jpg" 
                class="card-img-top" 
                alt="<%= product.title %>" 
                style=" height: auto; width: auto;"
                onerror="this.onerror=null; this.src='https://placehold.co/600x400/28a745/white?text=Proyecto&font=inter';"
              >
              <div class="card-body d-flex flex-column">
                <h5 class="card-title fw-bold">
                  <%= product.title %>
                </h5>
                <div class="card-footer bg-light border-0 pt-0 px-0">
                  <small class="text-muted">
                    <strong>Docente:</strong> <%= product.docente %><br>
                    <strong>Curso:</strong> <%= product.curso %>
                  </small>
                </div>
                <a href="/products/v1.0/detail/<%= product.id %>" class="btn btn-success mt-auto">
                  Ver más
                </a>
              </div>
            </div>
          </div>
        <% }); %>
      <% } else { %>
        <!-- Mensaje de "No encontrados" específico -->
        <div class="col-12 text-center">
          <p class="text-muted fs-4 mt-4">No se encontraron proyectos para "<%= query %>".</p>
          <a href="/products/v1.0/list" class="btn btn-outline-success">Volver a Explorar</a>
        </div>
      <% } %>
    </div>
  </div>
  
  <%- include('../partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
    xintegrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
    crossorigin="anonymous"></script>
</body>

</html>
